# Database Configuration for SQLite (local development)
DATABASE_URL="file:./prisma/dev.db"

# Database Adapter: 'mock' for testing, 'postgres' for production
# For SQLite: use 'postgres' (Prisma handles SQLite)
DATABASE_ADAPTER=postgres

# For development
NODE_ENV=development
PORT=4000
