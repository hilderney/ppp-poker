# PPP Poker — Development README

Quick start (node + npm required):

```bash
npm install
npm run dev
```

App: basic Alpha UI (single-user). Next steps: polish UI, add tests, then implement Beta (WebSocket multi-user sync).
